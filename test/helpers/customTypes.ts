export type UserDetails = {
  username: string;
  password: string;
};

export type Product = {
  title: string;
  price: number;
};

export type PaymentDetails = {
  firstName: string;
  lastName: string;
  zipCode: string;
};
